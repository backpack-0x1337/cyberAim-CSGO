

int main(){
    for loop (entity in entityList) {
        printf(entity + "health-offset"); // This will display all players health in game
        printf(entity + "location-offset"); // This will display all players location in game
        // we can illustrate player on our screen if we know where they are hence wallhack
    }
}
